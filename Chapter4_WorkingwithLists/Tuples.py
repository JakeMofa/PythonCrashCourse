dimensions = (200,50)
print(dimensions[0])
print(dimensions[1])


# looping through all values in a t
for dimension in dimensions:
    print(dimension)
    
#we cant modify a tuple but we can assign to a new value

dimensions = (400, 211)
for pt in dimensions:
    print(pt)
